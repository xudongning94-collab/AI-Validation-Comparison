# Project Baseline — v0.1.0-alpha.12

## 产品目标

面向投标人员，对 2–5 份 Word/PDF 投标文件进行多维度比对校验，形成结构化问题证据、风险评分与可定位结果；后续由 HiAgent/飞书承担交互和呈现。

## 架构边界

### Codex 工程承担

- 文档解析与结构化
- 干扰去除
- 文本、图片和格式比对
- AI 疑似度辅助分析
- 统一风险评分
- Finding / Report JSON
- Word 反向定位与批注
- HTTP API
- 自动化测试与 Benchmark

### HiAgent 承担

- 用户文件入口
- 工作流编排
- 并行/串行节点调度
- 少量 LLM 复核和报告节点
- 飞书机器人发布、卡片和结果展示

## 当前已完成

`v0.1.0-alpha.12 = D0–D12 Alpha + D13.1–D13.2 规则/候选检测 Alpha + H1–H4 本地接入包 Alpha`

```text
DOCX/PDF
  → DocumentIR（文本 + 图片特征 + 格式信息）
  → 干扰识别（exclude / downweight）
  → 文本候选召回 / 词法精排 / 干扰降权
  → 图片 SHA-256 / pHash / 颜色与宽高比辅助判断
  → 格式异常检查
  → AI 疑似度辅助分析（分数 + 置信度 + 信号 + 限制声明）
  → 统一风险评分（适用维度重归一化 + 贡献明细）
  → Finding / Comparison / Scoring JSON
  → DOCX 反向定位与原生批注副本 + Annotation JSON
  → FastAPI 版本化 HTTP 接口 / OpenAPI 文档
  → 隐私保护 Benchmark（业务角色 + 逻辑差异 + DOCX 包差异 + 性能 + 完整性）
  → HiAgent 接口契约与平台无关工作流规范
  → 单次解析的聚合分析接口 + 本地确定性报告
  → SQLite WAL 任务状态 + API Key 调用者隔离 + 哈希链审计
  → OCR/视觉独立 worker 证据契约
  → 签字签章规则引擎（存在性、主体/人员/日期/位置一致性）
  → OpenCV 红蓝印章候选 + 限定 ROI 签字墨迹候选
  → 飞书卡片 JSON 2.0 模板
  → 人工复核与发布门禁
```

## 算法边界

- D3 是可解释词法基线，尚未接入 Embedding 语义精排。
- D6 pHash 适合相同、压缩、缩放和轻微修改图片；复杂裁剪/重绘待视觉 Embedding。
- D7 是启发式风格辅助信号，不是 AI 检测证明，必须人工复核。
- D8 当前格式维度使用格式异常风险，跨文档格式相似度仍是增强项。
- D8 对不适用维度重新归一化权重，文档集总风险取最高文档风险，同时保留平均分。
- D9 当前仅写回 DOCX；PDF Finding 可以参与检测和评分，但不在 Alpha 阶段直接写入 PDF。
- D9 依赖 Finding 中的正文段落或表格单元格 locator；无法可靠定位的项会记录跳过原因，不做猜测式批注。
- D10 API 默认单文件上限 50 MB、每次最多 5 份，只接受 DOCX/PDF；批注接口只接受 DOCX，并始终返回新文件。
- D11 当前建立了真实样本执行与关系级阈值验证，但误报/漏报率仍需更多人工标注样本才能形成统计置信度。
- H1–H4 当前完成的是可版本化本地接入资产，不代表已经在真实 HiAgent 工作区或飞书租户发布。
- 主工作流现在调用 `/v1/analyze`，文件只解析一次；分项接口继续保留用于诊断和独立调用。
- OCR/视觉 worker 必须使用独立 Python 3.12 环境；模型权重尚未随主安装包离线封装。
- D13 只能检查签字签章的存在性、一致性、位置和明显质量异常，不能鉴定签名或印章真伪。
- 视觉证据未运行或失败时输出证据不可用，不得据此判定漏签或漏章。
- DOCX 当前原生文本按伪页 1 汇总；真实分页与精确坐标需完成页面渲染和 OCR worker。

## 当前验证

- `python -m compileall` 通过
- `pytest`：81/81 通过
- D3、D6、D7、D8、D9、D11 输出及 D10 错误响应通过对应 JSON Schema 校验
- D13 合规结果、视觉候选输出、API 输入边界及 worker 失败状态通过 Schema/自动化测试
- 现有 Python 3.12/OpenCV 环境通过合成签署页正向回归，识别出印章和签字候选
- D9 输出包含有效 Word 原生批注 OOXML，并验证源文件哈希保持不变
- 版本化业务接口、健康检查、OpenAPI 和输入边界通过集成测试
- D11 首组真实样本（1 份招标 PDF + 2 份响应 Word 版本）观测耗时 15.3–38.8 秒，源文件哈希不变；性能受缓存与并发负载影响
- 新旧版本正文序列与表格内容一致，但识别到 14 个 DOCX 包部件变化及 1 张同视觉哈希、不同分辨率图片
- H1–H4 的 OpenAPI 快照、工作流图、报告 Schema/Prompt 与飞书卡片模板通过契约测试

## 尚未完成

- D13.3 真实签署页样本标注、阈值校准和误报/漏报统计
- OCR/视觉模型权重离线封装、页面渲染和 worker 端到端回归
- D3 Embedding 语义精排（增强项）
- D6 视觉 Embedding 精排（增强项）
- 真实 HiAgent 工作区配置与飞书机器人端到端发布验收
- 更大规模人工标注样本的误报/漏报统计校准
