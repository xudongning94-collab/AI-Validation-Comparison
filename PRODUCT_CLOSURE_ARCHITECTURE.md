# 产品自闭环与合规检查架构

## 目标

在不依赖 HiAgent、飞书或外部 LLM 的条件下，单机即可完成：文件接收、解析、规则与视觉分析、风险评分、确定性报告、批注、任务留痕、失败恢复和结果下载。HiAgent/飞书是可选渠道，不是核心闭环依赖。

## 分层边界

```text
本地 UI / HiAgent / API 客户端
  → 鉴权与请求上下文
  → Task Orchestrator（状态机、幂等、恢复）
  → Core Pipeline（解析、比对、签章合规、评分）
  → Deterministic Reporter（无 LLM 也可交付）
  → Artifact Store（报告、批注、哈希、预览）
  → Audit Store（事件链、操作者、模型与规则版本）
```

外部 LLM 只可用于报告润色或低置信度复核，不能修改核心分数、Finding ID、定位或确定性报告原件。

## 任务上下文

每个任务必须有一个不可变的 `task_id` 和版本化上下文快照：

- `trace_id`、创建人/调用方、创建时间和租户边界
- 输入文件 ID、显示名、大小、SHA-256 和业务角色
- 招标文件、响应文件、新旧版本之间的显式关系
- 产品版本、规则集版本、阈值版本、OCR/视觉模型版本
- 每阶段输入指纹、状态、尝试次数、起止时间和错误码
- Finding、报告、批注等产物 ID、哈希和父子关系
- 人工确认、驳回、修订和重新运行记录

任何阶段不得仅凭文件名猜测新旧版本或业务角色。

## 状态机与恢复

```text
created → validating → parsing → analyzing → reporting → annotating → succeeded
             │           │           │           │
             └───────────┴───────────┴───────────┴→ failed
failed → retrying → 从最后一个可恢复检查点继续
```

要求：

1. 每阶段以 `task_id + stage + input_fingerprint + implementation_version` 生成幂等键。
2. 成功阶段保存结果哈希；重试时命中相同输入直接复用。
3. 原文件只读保存，任何批注或转换都写入新产物。
4. 错误分为输入错误、可重试基础设施错误、模型不可用和不可恢复内部错误。
5. 进程崩溃后把遗留 `running` 阶段标记为 `interrupted`，由恢复器重新排队。
6. OCR/视觉 worker 失败时保留核心文本结果，并把视觉阶段标记为部分失败，不能伪装为“未发现问题”。

## 本地持久化

Alpha 单机版采用 SQLite WAL + 文件产物目录：

- `tasks`：任务主状态、上下文快照和当前检查点
- `task_stages`：阶段状态、幂等键、输入/输出哈希和重试次数
- `audit_events`：追加写事件；使用前序哈希形成防篡改事件链
- `artifacts`：文件、报告、批注和预览的元数据及 SHA-256
- `findings`：Finding 索引、严重度、文档与定位，不复制不必要的正文
- `model_calls`：提供方、模型、用途、耗时、状态和成本；不记录密钥

二进制文件存放在 `data/tasks/<task_id>/`，数据库只保存受控相对路径。后续多实例部署可替换为 PostgreSQL + 对象存储，不改变领域契约。

## 鉴权与租户隔离

- 本机开发可显式使用 `auth_mode=disabled`，此模式只允许绑定回环地址。
- 内网/生产必须启用 API Key 或网关 JWT，默认拒绝匿名访问。
- 密钥只保存在环境变量或密钥管理系统中，数据库只记录不可逆身份摘要。
- 每次任务读取、下载、重试和人工确认都校验租户和任务所有权。
- `/health` 可匿名；OpenAPI、任务详情和文件下载按部署策略保护。

## 审计原则

每个审计事件至少包含：`event_id`、`task_id`、`stage`、`event_type`、`actor_id`、`timestamp`、`payload_hash`、`previous_event_hash`、`event_hash`。

审计日志不保存访问令牌、完整文件、完整正文或不必要的个人信息。需要定位时保存文档 ID、页码/段落/表格坐标和内容哈希。

## OCR 与视觉 worker

主 API 保持轻量 Python 运行时；OCR/视觉使用独立 Python 3.12 worker：

```text
DOCX/PDF → 页面渲染 → OCR worker / CV worker → 证据 JSON → 合规规则引擎
```

可复用资产：

- PaddleOCR 3.7 + PaddlePaddle 3.3，优先 PP-OCRv6 medium
- OCR 文本、四边形/边界框、置信度和低置信度标志的稳定输出结构
- OpenCV 边缘、轮廓和区域候选检测

不直接复用：

- PPT 净底图/图像生成后端；它是生成工具，不是合规证据检测器
- 未经人工确认的 OCR 文本作为公司名称、人员或日期的最终真值

worker 输出必须包含引擎、模型、版本、设备、输入哈希、页码、坐标、置信度、耗时和错误状态。

## 签字签章合规引擎

### 输入

- 招标要求抽取出的签署清单，或人工配置的规则集
- 投标主体标准名称、别名、法定代表人、授权代表和有效日期范围
- 页面渲染图、OCR 文字框、图像候选区域和原生 DOCX 对象信息

### 检查项

- 应签未签、应盖未盖、签章页缺失
- 公司名称缺失、错字、简称越界或与投标主体不一致
- 法定代表人/授权代表姓名缺失或角色混用
- 日期缺失、格式非法、晚于截止时间、早于授权时间或跨页矛盾
- 签字/印章偏离要求区域、跨页错位或遮挡关键信息
- 印章候选质量过低、疑似裁切、过度透明或无法 OCR
- 新旧版本签章区域发生变化

### 证据融合

1. 原生 DOCX 文本/结构优先。
2. 页面 OCR 用于补充扫描件和图片内文本。
3. 红色/蓝色印章候选、轮廓和局部视觉特征用于定位，不用于验真。
4. 可选 VLM 只复核低置信度候选，并输出带限制声明的结构化意见。
5. 最终 Finding 必须含页码/对象坐标、规则 ID、证据来源和置信度。

系统只能检查存在性、一致性、位置和明显异常，不能鉴定真实签名或印章真伪。

## 确定性报告

核心报告由代码生成并通过 JSON Schema 校验：

- 分数、风险级别、文档和 Finding ID 原样引用核心结果
- Finding 按严重度和分数稳定排序
- 不复制完整正文，不虚构证据
- 固定输出限制声明和人工复核要求
- 相同输入、规则和版本产生相同 `report_id`

LLM 报告只能基于确定性报告生成展示稿，必须保留原始报告 ID，并通过 Schema 和事实引用校验。

## 当前实现状态（v0.1.0-alpha.12）

- 已实现签署规则模型、证据状态、确定性合规引擎和 `/v1/signature-check`。
- 已实现独立 OpenCV worker，可检测红/蓝印章候选和指定 ROI 内的签字墨迹候选。
- 已验证成功、禁用和失败三类 worker 输出契约；视觉不可用不会被解释为漏签漏章。
- 已用合成签署页完成现有 Python 3.12/OpenCV 运行时正向回归。
- 尚未完成 DOCX/PDF 页面渲染、PaddleOCR 独立 worker、模型权重离线封装和真实签署页阈值校准。
- 因此 D13.1–D13.2 达到 Alpha 契约基线，真实业务可用性验收仍以 D13.3 为准。

## 实施顺序

1. D12.1：聚合分析接口 + 确定性报告。
2. D12.2：任务上下文、SQLite 状态机、审计事件链和恢复器。
3. D12.3：API Key/JWT 适配、产物下载和本地 UI。
4. D13.1：页面渲染、OCR worker 契约和公司名称/日期规则。
5. D13.2：签字/印章候选检测、位置规则和版本区域比对。
6. D13.3：真实样本标注、阈值校准和误报/漏报统计。
7. D14：可选 VLM/Embedding 网关与低置信度复核。
